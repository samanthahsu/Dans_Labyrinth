package model;

import java.util.ArrayList;

public abstract class InteractableList extends ArrayList {

    public boolean equals(InteractableList other) {
        if (this.size() == other.size()) {

        }
    }
}
